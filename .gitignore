build/
build-arm64/
build-dist/
dist/
*.so
*.a
*.o
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt

# stale / out-of-tree build output
build-tools/out/

# Python bytecode cache (offline analysis tools under tools/)
__pycache__/
*.pyc

# MSVC intermediates (not covered by the generic CMake patterns above)
*.obj
*.lib
*.pdb
*.tlog
*.ilk
*.exp

# Captures of real amps/pedals/cabs — licensed third-party content. NEVER commit:
# only the user's own captures may be distributed. (NAM neural models + IR files.)
*.nam
*.ir
*.wav
*.aidax
nam_refs/
nam_models/
ir_files/
ir_refs/

# Claude Code working files (never distribute)
.claude/
CLAUDE.md
CLAUDE.local.md

# Faceplate SOURCE material art (~2.5 MB each; regen-only inputs to make_faceplates.py).
# The OUTPUT faceplate-*.png shipped in lv2/modgui-hexforge/ ARE committed.
build-tools/materials/
build-tools/tubehex_src.png
desktop/out/
